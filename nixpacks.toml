[variables]
NODE_ENV = 'production'

[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm cache clean --force', 'npm install --production=false --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

# Static site deployment - no start phase needed